# MORNING — what changed overnight (experience lane, Jul 4→5)

*Read CRITIQUE.md first for the diagnosis. This is what shipped, why, and what
needs your eye. Everything below is committed and verified at 390px against
the live replay + the real stands service; typecheck green, console clean.*

## What shipped (walk rooot.club in this order)

1. **The door is a ticket.** Bureaucratic strip (THE TOURNAMENT · ARG–CPV ·
   JUL 3 · 22:00 UTC), the ROOOT masthead with the pop-ball O, two gate
   panels — drawn flag-blocks, tricodes shouted, press-black punch bands
   (ROOT ARG / ROOT CPV). No slogans; the choice explains itself. Epoch
   kickoffs can never print (`voice.ts` owns time facts: future = your
   clock, played = UTC record).

2. **The band is a masthead that becomes a HUD.** While the stage (which
   carries its own scoreboard) is on screen, the top band just names the
   object — ROOOT, feed state, PRINT. Scroll the stage away and the score +
   clock + team ticks print in. Kills the two-scoreboards-100px-apart
   incoherence; fits 390 with air. The band's PRINT cell (gold) flips the
   stage to its posed §10 frame ("Pose" the verb died — a fan prints a
   moment, they don't pose it). **The house ball spins one stepped
   revolution when the score turns.**

3. **The drum is a drum.** The pop-ball disc sits proud of the bar, black
   word-band across its face (ROOT before you pick, ROOOAR after), your
   end's ink on the ring once rooted. Every hit: squash, one hard pinwheel
   step (36°), a crisp op-art ring. No pill, no translucent fills.

4. **The ledger is a wire report.** CAPS voice; weight by TYPE not by
   border: GOOOL = a press-black band with cream knockout + the score stamp
   + the swing chip riding it; majors print fat with inked minute plates;
   minors are grey ink; folds are printed rules that wrap. The swing chip is
   the loudest data on any row now — Doto numbers with a DRAWN stepped
   arrow, edged in the gaining side's ink. Pending VAR = "GOAL?" + one
   CHECKING chip + the pulsing dot-fray underline (watched it live on the
   real replay at 6' — it holds breath correctly). The unread chip docks in
   the header band. The empty ledger shows blank ruled slots + the kickoff
   fact — the paper the match will print on.

5. **The page has a body now.** box-sizing reset (the shell used to overflow
   4px and scroll sideways — broken at every width). Weight scale
   frame/panel/detail instead of uniform hairlines. Grey inks are printed
   mixes — no alpha ink anywhere (the one legal ghost: offline local counts,
   grey italic). Emoji flags are gone from the shell — identity is drawn
   flag-blocks everywhere I own. The page ends with a colophon: wordmark ·
   fixture record · `MARKET · TXLINE ON SOLANA / CROWD · REAL TAPS, COUNTED`
   — the thesis as facts. Boot screen left the night look (newsprint + flat
   pop-ball, no glow, no serif).

6. **The switch path exists.** "YOU CAN SWITCH" was a promise with no path:
   end plates in the strip now reopen the door; your gate's punch fills
   GOLD (a punched ticket), the other stays open, a drawn × keeps your end.
   First visit stays undismissable — the door IS the entry.

## Needs your eye (the taste gates I can't self-serve)

- **The drum's size/pride** (88px, -26px proud of the bar). I think it's the
  grin of the page; if it reads toy-like to you, 76px flat-seated is the
  fallback. Judge on the phone.
- **GOOOL as the ledger's black band** — on the five-goal epic bundle this
  prints five black bands. I believe that's the right loudness for goals;
  if the page reads striped, the fallback is black bands for the FAN'S end
  only, sunbleach plates for the other end (I'd argue against — the ledger
  reports, it doesn't root).
- **"GOAL prints as GOOOL"** — house spelling of a wire fact. Honesty call
  parked for you.
- **The masthead↔HUD flip threshold** (stage top 14% + 64px). If it flips
  too eagerly/lazily on your phone, it's one number.

## Cross-lane notes (not mine to fix tonight)

- **Stage canvas, live mode**: leaves ~9% dead paper inside the window's
  bottom edge (posed mode fills it). I hug it with a 0.615 aspect box, but
  the canvas should either fill its live box or export its preferred aspect.
- **Stage canvas scoreboard**: ARG renders as an EMOJI flag, CPV as a drawn
  block — mixed identity language on one band (emoji is anti-print; the
  shell no longer uses any).
- **Both ARG and CPV territories are blue-family** — the tide's two inks are
  near-neighbors this fixture. The ground/territory rotation may want a
  hue-distance rule.
- **Dev harness only**: `?ledger` feeds AUS–EGY odds and ARG–CPV replay odds
  into one builder — swings can cross-correlate in dev. A fixture filter on
  `pushOdds` would clean it.
- **Faith chip** (CHEERS COUNT DOUBLE over the drum) is positioned but
  untested against a live faith state from the service.
- Desktop (≥900px) verified numerically (band/columns/measure) + small-scale
  visual only — the capture tool wouldn't render 1280 trustworthily tonight.
  The broadsheet needs a real composition pass tomorrow.

## What tomorrow should bring

1. **The epic bundle rehearsal** — run the five-goal ARG–CPV replay through
   the shell start-to-finish; tune the GOOOL band, the ball-spin, and swing
   chip timing against real drama density.
2. **The desktop broadsheet** — compose it properly (it's a stretched phone
   today, measure notwithstanding).
3. **The PRINT moment** — the band's PRINT flips the pose; the poster
   crystallization (relic printers) should hang off that gesture; that's the
   collect loop closing.
4. **USA–Belgium Jul 7** is the hero fixture — the door, the band, and the
   colophon all read fixture facts from data, so they're ready; the tide's
   two inks (see hue-distance note) are the risk to check.

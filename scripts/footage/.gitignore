# footage output -- video segments + event timelines are gigabytes of binary;
# they never enter git (same rule as fixtures/*.jsonl). node_modules/ is
# already covered by the repo-root .gitignore.
out/

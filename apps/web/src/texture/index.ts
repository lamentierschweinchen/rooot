/** ROOOT texture lane — the derived threads (POSSESSION · PRESSURE · TEMPO). */
export { TextureBuilder } from './TextureBuilder';
export type { Spell, TextureSample, TextureSnapshot } from '@contracts/texture';

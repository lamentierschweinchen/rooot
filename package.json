{
  "name": "rooot-ops",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ROOOT root package: ops scripts (TxLINE auth, recording) + repo-wide tooling. The web app lives in apps/web, the stands service in services/stands, the frozen seams in contracts/.",
  "scripts": {
    "dev": "npm --prefix apps/web run dev",
    "build": "npm --prefix apps/web run build",
    "typecheck": "tsc --noEmit && npm --prefix apps/web run typecheck && npm --prefix services/stands run typecheck",
    "spike": "tsx scripts/txline-spike.ts",
    "subscribe": "tsx scripts/txline-subscribe.ts",
    "record": "tsx scripts/record.ts",
    "inspect": "tsx scripts/replay-inspect.ts"
  },
  "dependencies": {
    "@coral-xyz/anchor": "0.32.1",
    "@solana/spl-token": "0.4.14",
    "@solana/web3.js": "1.98.4",
    "tweetnacl": "1.0.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "4.22.4",
    "typescript": "^5.7.0"
  }
}
